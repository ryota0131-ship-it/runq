import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.astome.runq',
  appName: 'RUNQ.',
  webDir: 'dist',
  server: { iosScheme: 'https' }
};

export default config;
